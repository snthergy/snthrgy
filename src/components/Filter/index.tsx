import * as React from "react";
import {SectionContainer} from "../sharedStyles";

export interface IFilterProps {}

export default function Filter(props: IFilterProps) {
  return <SectionContainer>Filter</SectionContainer>;
}
