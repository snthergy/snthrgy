import * as React from "react";
import {SectionContainer} from "../sharedStyles";

export interface IAmpProps {}

export default function Amp(props: IAmpProps) {
  return <SectionContainer>Amp</SectionContainer>;
}
